import{_ as a,c as o,o as t,aj as i}from"./chunks/framework.BfUM8hhE.js";const f=JSON.parse('{"title":"Migration Guide","description":"","frontmatter":{},"headers":[],"relativePath":"reference/migration.md","filePath":"reference/migration.md","lastUpdated":null}'),r={name:"reference/migration.md"};function s(n,e,d,l,c,h){return t(),o("div",null,[...e[0]||(e[0]=[i('<h1 id="Migration-Guide" tabindex="-1">Migration Guide <a class="header-anchor" href="#Migration-Guide" aria-label="Permalink to &quot;Migration Guide {#Migration-Guide}&quot;">​</a></h1><h2 id="Version-0.4.0" tabindex="-1">Version 0.4.0 <a class="header-anchor" href="#Version-0.4.0" aria-label="Permalink to &quot;Version 0.4.0 {#Version-0.4.0}&quot;">​</a></h2><p>This version saw two big changes, the possibility of using native Julia packages as backends for reading and writing geospatial data, and the switch to GeometryOps.jl for geometry operations.</p><h3 id="Native-Julia-Backends" tabindex="-1">Native Julia Backends <a class="header-anchor" href="#Native-Julia-Backends" aria-label="Permalink to &quot;Native Julia Backends {#Native-Julia-Backends}&quot;">​</a></h3><p>When a native Julia package for a specific geospatial format is installed and imported (e.g. also loading Shapefile.jl), GeoDataFrames.jl will automatically use it as the backend for reading and writing files of that format (e.g. .shp files). This allows for faster and more efficient handling of geospatial data without relying on ArchGDAL. See <a href="/GeoDataFrames.jl/stable/tutorials/formats#File-formats">File formats</a> for all native drivers and formats.</p><p>This means that the keyword arguments for <code>read</code> and <code>write</code> may differ from those when using the default (and pre v0.4.0) ArchGDAL backend. Please refer to the documentation of the corresponding package for details.</p><p>You can get the old behaviour back by explicitly using the <code>ArchGDALDriver</code> when reading/writing files like so:</p><ul><li><p><code>read(GeoDataFrames.ArchGDALDriver(), fn; kwargs)</code></p></li><li><p><code>write(GeoDataFrames.ArchGDALDriver(), fn, df; kwargs)</code>.</p></li></ul><h3 id="Geometry-operations" tabindex="-1">Geometry operations <a class="header-anchor" href="#Geometry-operations" aria-label="Permalink to &quot;Geometry operations {#Geometry-operations}&quot;">​</a></h3><p>With native drivers, as discussed above, geometries may no longer be of ArchGDAL types, but native Julia geometries. While this might speed up your code, most of these native driver packages do not implement geometry operations (e.g. <code>intersects</code>). Using GeoInterface operations on them will then lead to a <code>MethodError</code>, as it only is implemented by and for ArchGDAL.jl. Therefore, GeoDataFrames.jl now depends on GeometryOps.jl and exports its methods for geometry operations. GeometryOps operations will also work for the default ArchGDAL geometries.</p><p>To get the old behaviour back, you can still use GeoInterface operations but need to use the <code>read/write(ArchGDALDriver(),...)</code> discussed above.</p>',11)])])}const m=a(r,[["render",s]]);export{f as __pageData,m as default};
